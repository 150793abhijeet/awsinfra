#########################################################################################################################################
#						VPC DELETION
#########################################################################################################################################
- name: Remove vpc
  local_action:
    module:   ec2_vpc
    state: "absent"
    vpc_id: "{{ vpc_id }}"
    region: "{{ region }}"
    resource_tags: "{}"
    wait : yes
