######################################################################################################################
##               removing internet gateway
#################################################################################################################
- name: Remove internet gateway
  local_action:
    module: ec2_vpc_igw
    vpc_id: "{{ vpc_id }}"
    state: "absent"
    region: "{{ region }}"
